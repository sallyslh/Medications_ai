# Core framework
Django>=4.2,<5.1
djangorestframework>=3.15,<4.0

# Database
psycopg2-binary>=2.9

# Authentication
djangorestframework-simplejwt>=5.3

# CORS
django-cors-headers>=4.3

# Environment config
python-decouple>=3.8

# ML / data science (for drug prediction engine)
# Pinned exactly (not >=) to match the versions complete_drug_model_system.pkl
# was trained with in Colab (scikit-learn 1.6.1). A newer scikit-learn/numpy
# loads the pickle without erroring but can silently change TfidfVectorizer /
# TruncatedSVD numerics, producing different — sometimes nonsensical —
# recommendations for the same input. See drugbank_model/loader.py.
scikit-learn==1.6.1
numpy==1.26.4
scipy==1.13.1
pandas==2.2.3
joblib==1.4.2

# Production server
gunicorn>=21.2

# Utilities
python-dateutil>=2.9
